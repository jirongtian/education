package com.ntyx.vedu.mapper;import com.ntyx.mapper.CommonMapper;import com.ntyx.vedu.entity.Course;import com.ntyx.vedu.entity.Title;import com.ntyx.vedu.entity.User;import java.util.List;public interface TitleMapper extends CommonMapper<Title> {    //查询全部title题目    List<Title> findAll();    //保存    int save(Title title);    //根据id查询    Title findById(Integer titleId);    //根据id删除    int deleteById(Integer titleId);    //更新    int update(Title title);}