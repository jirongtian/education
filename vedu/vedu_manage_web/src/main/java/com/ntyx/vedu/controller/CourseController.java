package com.ntyx.vedu.controller;import com.github.pagehelper.PageInfo;import com.ntyx.vedu.entity.Course;import com.ntyx.vedu.service.CourseService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.stereotype.Controller;import org.springframework.web.bind.annotation.GetMapping;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RequestParam;@Controller@RequestMapping("/course")public class CourseController {    @Autowired    private CourseService courseService;    /**     * 查询课程表数据     */    @RequestMapping(value = "/list" , name = "课程查询")    public ResponseEntity<PageInfo> list(@RequestParam(defaultValue = "1") Integer page , @RequestParam(defaultValue = "10") Integer pageSize){        //分页数据        PageInfo pageInfo = courseService.findAll(page, pageSize);        return ResponseEntity.ok(pageInfo);    }    /**     * 保存或修改     */    @RequestMapping(value = "/edit" ,name = "保存或修改" )    public ResponseEntity<Void> edit(Course course) throws Exception {        //如果id为null说明新增        if(course.getId()==null){            courseService.save(course);        }else{            courseService.update(course);        }        return new ResponseEntity<>(HttpStatus.CREATED);    }    /**     * 删除课程     */    @RequestMapping(value = "/delete" ,name = "删除课程")    public ResponseEntity<String> delete(@RequestParam(name = "id") Integer id ){        courseService.delete(id);        String msg="success";        return  ResponseEntity.ok(msg);    }    /**     * 跳转用户修改页面     */    @GetMapping(value = "/toUpdate" ,name = "课程修改页面" )    public ResponseEntity<Course> FindUserById(@RequestParam(name = "id") Integer id){//根据id查询需要修改的数据        Course course = courseService.findById(id);        return ResponseEntity.ok(course);    }}