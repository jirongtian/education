package com.ntyx.vedu.mapper;import com.ntyx.mapper.CommonMapper;import com.ntyx.vedu.entity.Course;import com.ntyx.vedu.entity.User;import java.util.List;public interface CourseMapper extends CommonMapper<Course> {    //查询全部课程    List<Course> findAll();    //保存    int save(Course course);    //根据id查询    Course findById(Integer courseId);    //根据id删除    int deleteById(Integer courseId);    //更新    int update(Course course);}